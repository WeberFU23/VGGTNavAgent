# 多目标导航 Benchmark 设计方案

代码：[Multi-Object-Navigation-Benchmark](https://github.com/WeberFU23/Multi-Object-Navigation-Benchmark)

## 1. 研究动机

### 1.1 现有 Benchmark 的局限

以 MultiON、GOAT 为代表的多目标导航 benchmark，本质上是将"多目标导航"简化为"给定顺序的多次单目标导航"：

- 目标顺序由指令显式给出，agent 只需按序执行 "go to A → go to B → go to C"
- 每个子任务的决策空间退化为单目标导航问题（如何到达下一个已知目标）
- Agent 不需要在多个候选目标间做取舍、排序、路径整体规划
- 对 working memory 的要求很弱：只需记住"当前子目标是谁"，无需维护"已找到哪些"和"去过哪里"的复杂状态

这类设计更接近**执行能力**测评，模型只需按指令依次执行，决策空间小，难以体现**规划能力**。

### 1.2 本 Benchmark 的定位

任务的核心变化是：**目标顺序不给定，数量可能不给定**，agent 必须自主完成：

1. **搜索规划**：决定访问目标（或候选区域）的顺序与路线，这是一个类 TSP / Orienteering 的组合优化问题
2. **状态追踪**：要求 agent 维护"空间记忆"（去过哪里、哪里还没探索）与"目标记忆"（找到过什么、是否重复）
3. **终止决策**（仅限找全任务）：在不知道目标总数的情况下，自主判断"是否已经找完"并决定停止

这三项能力在 MultiON/GOAT 中都不构成有效决策空间，是本 benchmark 与现有工作的核心区别。

## 2. 任务定义

实现中的模式标记与本文术语的对应关系：`many` = MOS，`all` = MOC；另有 `any`（找到任意一个实例）作为基线/冒烟模式，不参与主榜单。

### 2.1 Task 1 — Multi-Object Search（MOS / `many`，多目标搜索）

**定义**：给定目标类别集合 $C=\{c_1,...,c_n\}$ 与数量要求 $k$，要求 agent 在预算步数 $T$ 内找到 $k$ 个满足类别要求的**互不相同**的目标实例（在实例附近发出找到信号）。

- **变体 A（同类多实例）**：如"找到 3 个杯子"，环境中可能存在 $M>k$ 个杯子实例，agent 需要在 $M$ 个候选中选出一个子集去访问——这里天然引入了"目标选择"这一规划子问题，而不仅是路径规划
- **变体 B（跨类别，各 1 个）**：如"找到冰箱、沙发、台灯"，顺序不限，测试纯路径规划（TSP-like）
- **变体 C（数量 + 跨类别组合）**：如"找到 2 把椅子和 1 台电视"，兼具子集选择与路径规划，难度最高

MOS 的关键特性：**目标数量已知，但顺序未知**，因此不涉及终止判断的不确定性，主要考察搜索顺序规划与（变体 A/C 下的）目标选择能力。找齐后是否立即终止不单独评分，只会通过路径长度在 SPL-multi 中被间接反映（见 3.2 注）。

### 2.2 Task 2 — Multi-Object Collection（MOC / `all`，多目标收集/找全）

**定义**：给定目标类别集合，要求 agent 找到环境中该类别的**全部**实例（总数 $N$ 对 agent 未知），并在确信找全后主动发出终止信号。若在预算 $T$ 内未主动终止，则在耗尽预算时强制截断。

MOC 相较 MOS 多了一层核心难点：**没有数量提示，agent 必须自己判断探索是否已经充分**，这是现有 benchmark 完全没有覆盖的能力维度，也是 MOC 的成功定义中显式要求主动终止信号的原因。

### 2.3 分层变量

- 目标数量 $k$ / $N$：由类别在场景中的目标簇数 $n$ 决定——$n \le 4$ 时 $k$ 依次取 $2,...,n$；$n>4$ 时从 $[2,n]$ 中随机取 3 个互不相同的数（种子固定，见 `dataset_creation/build_queries.py` 的 `many_target_counts`）
- 目标描述方式：类别名（object）/ 自然语言（description，简单或复杂的语言约束）/ 目标图片（image，给出标注了目标框的参考图）

---

## 3. 评测指标体系

指标体系分三层，从"是否做对"到"做得多好"再到"为什么没做好"，逐层深入。三层之间尽量避免同一现象被多个指标重复计分。

### 3.1 指标体系总览

共 8 个指标、三层（SR、P、R、F1、PE、TSE、SPL-multi、RTSR），每层对应一种能力侧面，层内指标互为补充、尽量避免重复计分（如 RTSR 与 Precision 都涉及重复上报，二者相关但粒度不同，见 3.4）。主榜单只呈现 **SR、F1、SPL-multi** 三项作为跨模型排名的核心指标，其余作为细粒度分析表，用于诊断失败原因与能力归因。

### 3.2 第一层：任务完成度（结果层）

**Success Rate（SR）**

- MOS：预算内找到 $\ge k$ 个去重后的合法目标实例，记为成功。
- MOC：找到全部 $N$ 个合法实例，**且** agent 主动发出完成信号（超时被强制截断不算成功）。

注：MOS 的终止行为不单独评分——由于目标数量已知，找齐后的终止判断是平凡的；找齐后继续游荡只会通过实际路径长度 $p_i$ 变长在 SPL-multi 中被间接惩罚。终止决策的显式考核仅存在于 MOC。

**Precision / Recall / F1**

设 agent 发出的"目标已找到"信号集合为 $\hat{Y}$，ground truth 合法目标集合为 $Y$：

- TP：信号对应一个此前未被认领过的真实目标实例
- FP：信号对应错误实例，或重复认领了已经报告过的同一实例（超出集群配额的部分）
- FN：episode 结束时仍未被任何信号覆盖的应找实例

两个任务的 $Y$ 与 Recall 分母分别定义：

- **MOS**：任务只要求 $k$ 个实例，因此 $R=\frac{TP}{k}$（$TP$ 封顶为 $k$，超出 $k$ 的正确上报不计入也不惩罚），FN $= k - TP$。这样正确完成任务的 agent Recall 恒为 1，不会因环境中存在多余同类实例而被惩罚。
- **MOC**：$Y$ 为环境中该类别的全部合法实例，$|Y|=N$，$R=\frac{TP}{N}$，FN $= N - TP$。

$$P=\frac{TP}{TP+FP},\quad F_1=\frac{2PR}{P+R}$$

**计数口径约定**（与评测实现一致）：

- TP/FP 按**被认领的目标实例**计数，而非按信号次数计数：一次"找到"信号若同时处于多个目标附近，会同时认领这些目标，分别计入 TP/FP。
- 当 agent 在整个 episode 中未发出任何有效认领（$TP+FP=0$）时，Precision 按 0 计（此时 Recall 也为 0，F1=0）。

**反映的能力：**

- SR 是二值指标，两个模型都失败时无法区分能力差距，因此必须搭配 P/R/F1 报告部分完成情况
- Precision 的引入直接防住了一种常见的 reward-hacking 策略：如果只看 Recall，agent 可以靠"逢同类物体就上报"把 Recall 刷满；Precision 会惩罚这种策略，因为重复认领同一目标或误报都会拉低 Precision
- **Precision 同时是"目标记忆"能力的直接证据**：重复上报同一个已认领目标，说明 agent 没有正确维护"已找到过什么"的状态

### 3.3 第二层：路径效率（规划质量层）

**路径执行效率（Path Efficiency, PE）**——衡量"给定 agent 实际选择的子集，走的路相对该子集下最优路径的效率"：

$$PE_i = \frac{l_i^{\text{agent-subset}}}{p_i}$$

这里 $l_i^{\text{agent-subset}}$ 是针对 agent 选择的子集求解的最优路径。对于 MOC（无子集选择，TSE=1 恒成立），PE 就是 SPL-multi 的唯一路径效率因子。

**目标选择质量（Target Selection Efficiency, TSE）**——仅用于 MOS 变体 A/C（存在同类多实例选择空间）：

$$TSE_i = \frac{l_i^{\text{optimal-subset}}}{l_i^{\text{agent-subset}}}$$

$l_i^{\text{optimal-subset}}$ 为全局所有合法 $k$-子集中路径最短的那一个（子集选择意义上的最优解），$l_i^{\text{agent-subset}}$ 为 agent 实际选择子集对应的最优路径。对于 MOC（无子集选择）与 MOS 变体 B（子集被任务唯一确定），TSE=1 恒成立。

**多目标 SPL（SPL-multi）**

$$\text{SPL-multi}=\frac{1}{M}\sum_{i=1}^{M} S_i\cdot\frac{l_i}{\max(p_i,l_i)} = \frac{1}{M}\sum_{i=1}^{M} S_i\cdot PE_i \cdot TSE_i$$

$S_i$ 为是否成功完成任务，$p_i$ 为 agent 实际路径长度，$l_i = l_i^{\text{optimal-subset}}$ 为所有合法目标子集中求解得到的最短路径（开路径 TSP 问题）。

TSE 和 PE 作为细粒度分析层，两个乘数分别归因"选得好不好"和"走得好不好"，而且乘积关系是精确的数学恒等式（$\frac{l^{\text{agent-subset}}}{p_i}\cdot\frac{l^{\text{optimal-subset}}}{l^{\text{agent-subset}}}=\frac{l^{\text{optimal-subset}}}{p_i}$）。

**最优路径的计算**：所有最优参考路径（$l_i^{\text{optimal-subset}}$ 与 $l_i^{\text{agent-subset}}$）在评测时用同一套参考点（目标最近 viewpoint 的 geodesic 距离）在线计算，保证 SPL/PE/TSE 内部口径一致。规模上，子集 TSP 在目标数 $\le 12$ 时用 Held-Karp 动态规划精确求解，超过时用贪心近似；数据集中预计算的 `shortest_path_distance`（基于吸附 nav 点）仅作为无 navmesh 时的兜底，不参与正式计算。使用贪心近似的 episode 会使恒等式只在近似精度内成立，分析时应按 `route_solver` 字段区分。

**反映的能力：**

- TSE 与 PE 解耦了两类失败原因：SPL-multi 低可能是"选对了目标但走得绕"，也可能是"目标选得不合理导致理论最短路径本身就长"
- TSE 直接对应"搜索规划"中的**子集选择**这一子能力——这是现有 MultiON/GOAT 完全不存在的决策维度（顺序、数量都给定时无需做任何选择）
- $l_i$ 本身如果因为场景拓扑（如唯一走廊）而包含必要的往返，这部分会自动被计入"最优"路径长度，因此 SPL-multi 不会冤枉那些老实探索复杂拓扑环境的 agent
- **在能够自行选择目标和访问顺序的规则下，SPL-multi 是"空间记忆"能力的直接证据**

### 3.4 第三层：目标记忆诊断

**目标重复选择率（Repeated Target Selection Rate, RTSR）**

$$RTSR=\frac{m-|unique(S)|}{m}$$

设 agent 发出找到信号对应的目标实例序列为 $S=(s_1,...,s_m)$，其中 $s_i$ 为真实目标实例 ID。约定 $m=0$（agent 未发出任何找到信号）时 RTSR 记为 0（无重复可计）。

RTSR 衡量 agent 是否能够维护"已找到目标"的状态。RTSR 越高，说明 agent 越容易重复选择已经访问过的目标，反映目标记忆与状态追踪能力不足。

**反映的能力**：

- RTSR 直接衡量 agent 是否能够维护"已完成目标集合"这一目标状态，是目标记忆（Goal Memory）能力的直接体现
- Precision 将重复选择和误识别统一视为 False Positive，而 RTSR 将重复选择单独统计，使两类错误能够分别归因。因此二者相关但不冗余：Precision 是"上报质量"的汇总指标，RTSR 专门隔离出"状态追踪失败"这一类错误
- 这一层是 MultiON/GOAT 完全不存在的评测维度：这两个 benchmark 的目标顺序显式给定，agent 无需维护"哪些目标已完成"的状态，自然不存在重复认领问题

### 3.5 聚合方式

所有指标先逐 episode 计算，再聚合：

- **宏平均**（默认）：对各 episode 的指标值取算术平均，每个 episode 权重相等。PE/TSE 在无定义（agent 未选出完整子集）的 episode 上记为 null，聚合时跳过。
- **微平均**（P/R/F1 与 RTSR 同时报告）：先跨 episode 汇总计数（TP/FP/应找数、重复上报数/总上报数）再算比率，目标多的 episode 权重更大。
- **按模式拆分**：`any` 只报 SR 和 SPL（单目标任务中 P/R/F1 退化为与 SR 同步的 0/1）；`many`/`all` 分别报告全套指标。
- **主榜单**（SR、F1、SPL-multi）只聚合 `many`/`all` episode；`any` 的结果单独列出，不混入总指标。

## 4. 与现有 Benchmark 的对比

用状态空间大小做更形式化的论证：

MultiON/GOAT 本质是线性序列执行问题，决策空间随目标数量线性增长；本 benchmark 的 MOS 变体 A/C 是子集选择 + 路径规划的组合问题，决策空间随候选目标数量组合爆炸增长（$\binom{M}{k}$ 种子集选择 × 每种子集的排列可能），这是"决策空间更大、更能测出规划能力"这一论断的定量依据。

## 5. 小结

- **测评搜索规划能力**：模型需要自主决定搜索顺序和路线，而不是执行给定序列
- **测评状态追踪能力**：模型需要同时记录"去过哪里"和"找到过哪些目标"，对 working memory 有更高要求
- **更贴近真实任务**：现实中的导航指令（"找到所有的杯子"）天然是无序的
