# 多目标导航 Benchmark 设计方案

代码：[Multi-Object-Navigation-Benchmark](https://github.com/WeberFU23/Multi-Object-Navigation-Benchmark)

## 1. 研究动机

### 1.1 现有 Benchmark 的局限

以 MultiON、GOAT 为代表的多目标导航 benchmark，本质上是将"多目标导航"简化为"给定顺序的多次单目标导航"：

- 目标顺序由指令显式给出，agent 只需按序执行 "go to A → go to B → go to C"
- 每个子任务的决策空间退化为单目标导航问题（如何到达下一个已知目标）
- Agent 不需要在多个候选目标间做取舍、排序、路径整体规划
- 对 working memory 的要求很弱：只需记住"当前子目标是谁"，无需维护"已找到哪些"和"去过哪里"的复杂状态

这类设计更接近**执行能力**测评，模型只需按指令依次执行，决策空间小，难以体现**规划能力**。

### 1.2 本 Benchmark 的定位

任务的核心变化是：**目标顺序不给定，数量可能不给定**，agent 必须自主完成：

1. **搜索规划**：决定访问目标（或候选区域）的顺序与路线，这是一个类 TSP / Orienteering 的组合优化问题
2. **状态追踪**：要求 agent 维护"空间记忆"（去过哪里、哪里还没探索）与"目标记忆"（找到过什么、是否重复）
3. **终止决策**（仅限找全任务）：在不知道目标总数的情况下，自主判断"是否已经找完"并决定停止

这三项能力在 MultiON/GOAT 中都不构成有效决策空间，是本 benchmark 与现有工作的核心区别。

## 2. 任务定义

实现中的模式标记与本文术语的对应关系：`many` = MOS，`all` = MOC；另有 `any`（找到任意一个实例）作为基线/冒烟模式，不参与主榜单。

### 2.1 Task 1 — Multi-Object Search（MOS / `many`，多目标搜索）

**定义**：给定目标类别集合 $C=\{c_1,...,c_n\}$ 与数量要求 $k$，要求 agent 在预算步数 $T$ 内找到 $k$ 个满足类别要求的**互不相同**的目标实例（在实例附近发出找到信号）。

- **变体 A（同类多实例）**：如"找到 3 个杯子"，环境中可能存在 $M>k$ 个杯子实例，agent 需要在 $M$ 个候选中选出一个子集去访问——这里天然引入了"目标选择"这一规划子问题，而不仅是路径规划
- **变体 B（跨类别，各 1 个）**：如"找到冰箱、沙发、台灯"，顺序不限，测试纯路径规划（TSP-like）
- **变体 C（数量 + 跨类别组合）**：如"找到 2 把椅子和 1 台电视"，兼具子集选择与路径规划，难度最高

MOS 的关键特性：**目标数量已知，但顺序未知**，因此不涉及终止判断的不确定性，主要考察搜索顺序规划与（变体 A/C 下的）目标选择能力。找齐后是否立即终止不单独评分；找齐前的移动效率由 SPL-multi 衡量（其路径只计到最后一次成功发现为止，见 3.3），找齐后游荡在 MOS 下不影响任何指标。

### 2.2 Task 2 — Multi-Object Collection（MOC / `all`，多目标收集/找全）

**定义**：给定目标类别集合，要求 agent 找到环境中该类别的**全部**实例（总数 $N$ 对 agent 未知），并在确信找全后主动发出终止信号。若在预算 $T$ 内未主动终止，则在耗尽预算时强制截断。

MOC 相较 MOS 多了一层核心难点：**没有数量提示，agent 必须自己判断探索是否已经充分**，这是现有 benchmark 完全没有覆盖的能力维度，也是 MOC 的成功定义中显式要求主动终止信号、并单设 Stopping Regret 指标（见 3.5）的原因。

### 2.3 分层变量

- 目标数量 $k$ / $N$：由类别在场景中的目标簇数 $n$ 决定——$n \le 4$ 时 $k$ 依次取 $2,...,n$；$n>4$ 时从 $[2,n]$ 中随机取 3 个互不相同的数（种子固定，见 `dataset_creation/build_queries.py` 的 `many_target_counts`）
- 目标描述方式：类别名（object）/ 自然语言（description，简单或复杂的语言约束）/ 目标图片（image，给出标注了目标框的参考图）

---

## 3. 评测指标体系

指标体系分两层：**主榜单**用于跨模型排名，只含三项；**诊断层**不进排名，用于定位失败原因与能力归因。设计上尽量避免同一现象被多个指标重复计分（例如找齐后的浪费移动只由 Stopping Regret 计，不再进入路径效率的分母）。

### 3.1 主榜单（跨模型排名）

**Success Rate（SR）**——任务是否成功：

- MOS：预算内找到 $\ge k$ 个去重后的合法目标实例。
- MOC：找到全部 $N$ 个合法实例，**且** agent 主动发出完成信号；找齐但被预算/超时截断不算成功。
- any：找到任意 1 个合法实例且零误报（只作基线，不进主榜聚合）。

**Precision / Recall / F1**——上报的准确与完整：

设 agent 发出的"目标已找到"信号集合为 $\hat{Y}$，ground truth 合法目标集合为 $Y$：

- TP：信号对应一个此前未被认领过的真实目标实例
- FP：信号对应错误实例，或重复认领了已经报告过的同一实例（超出集群配额的部分）
- FN：episode 结束时仍未被任何信号覆盖的应找实例

两个任务的 Recall 分母分别定义：

- **MOS**：任务只要求 $k$ 个实例，因此 $R=\frac{TP}{k}$（$TP$ 封顶为 $k$，超出 $k$ 的正确上报不计入也不惩罚），FN $= k - TP$。这样正确完成任务的 agent Recall 恒为 1，不会因环境中存在多余同类实例而被惩罚。
- **MOC**：$Y$ 为环境中该类别的全部合法实例，$|Y|=N$，$R=\frac{TP}{N}$，FN $= N - TP$。

$$P=\frac{TP}{TP+FP},\quad F_1=\frac{2PR}{P+R}$$

**计数口径约定**（与评测实现一致）：

- TP/FP 按**被认领的目标实例**计数，而非按信号次数计数：一次"找到"信号若同时处于多个目标附近，会同时认领这些目标，分别计入 TP/FP。
- 当 agent 在整个 episode 中未发出任何有效认领（$TP+FP=0$）时，Precision 按 0 计（此时 Recall 也为 0，F1=0）。

**SPL-multi（transit 口径）**——路径效率综合分：

$$\text{SPL-multi}_i = S_i\cdot\frac{l_i^{optimal}}{\max(p_i^{transit},\,l_i^{optimal})}$$

$S_i$ 为是否成功；$l_i^{optimal}$ 为全部合法目标实例上"最优子集 + 最优顺序"的最短路径；$p_i^{transit}$ 为 agent 从出发到**最后一次成功发现**之间实际走过的累计路径——找齐之后到终止之间的移动不计入，避免与 Stopping Regret 对同一段浪费重复扣分。$found=0$ 的 episode 因 $S_i=0$ 自然归零。

**这三项反映的能力**：SR 是二值指标，两个模型都失败时无法区分能力差距，因此必须搭配 P/R/F1 报告部分完成情况；Precision 同时防住"逢同类物体就上报"刷 Recall 的策略，也是目标记忆的直接证据；SPL-multi 在目标与顺序自主选择的规则下是空间记忆与路径规划的综合体现。

### 3.2 诊断层之一：路径质量四因子

把 SPL-multi 的分子分母比分解为四个因子，每个对应单一、干净的能力归因。涉及五个路径量（起点均为 episode 实际起点，点对点距离用 geodesic 最短路，参考点优先目标 viewpoint 否则质心）：

| 路径量 | 含义 |
|---|---|
| $l^{optimal}$ | 全集合法实例上，最优子集 + 最优顺序的最短路 |
| $l^{pool\text{-}optimal}$ | 只在 agent **发现过**的实例池（见 3.6）里求解的最优子集 + 最优顺序 |
| $l^{agent\text{-}subset}$ | agent 实际上报的子集固定，重解最优顺序的最短路 |
| $g^{agent\text{-}order}$ | 按 agent 实际上报顺序逐段 geodesic 求和 |
| $p^{transit}$ | 到最后一次成功发现为止的实际路径长度 |

四个因子：

- **池质量** $= l^{optimal} / l^{pool\text{-}optimal}$：搜索/感知发现的候选池够不够好（纯搜索维度）。
- **TSE（目标选择效率）** $= l^{pool\text{-}optimal} / l^{agent\text{-}subset}$：在 agent 已知候选里选的子集是不是最优（纯选择判断，不会因没搜到藏得深的实例被冤枉扣分）。
- **OE（访问顺序效率）** $= l^{agent\text{-}subset} / g^{agent\text{-}order}$：选好的目标，访问顺序排得好不好。
- **LE（移动执行效率）** $= g^{agent\text{-}order} / p^{transit}$：按定好的顺序走得直不直。

**适用性规则**（不适用输出 null、聚合时跳过）：

- 池质量与 TSE 需要 agent 实现发现池接口 `get_target_pool()`（见 3.6），否则为 null，**不会静默退化为全集**（那会恒等于 1）。
- MOC 无子集选择（需求数 == 候选全集），TSE 恒不适用；MOS 变体 B/C 中需求数等于候选数时同理。
- 目标类别在环境中只有唯一实例时池质量不适用；单目标 episode 无"顺序"，OE 不适用；$found=0$ 时四因子全部不适用。
- 池覆盖不足需求数量时用池内全部实例求解并记录 `pool_covers_required=false`，此时池质量可能大于 1，保留该信号不截断。

**乘积校验**：四因子相乘在数学上恒等于 $l^{optimal}/p^{transit}$（telescoping），聚合时自动校验，但只在四因子齐全且所有中间量均为精确 TSP 解的 episode 上严格验证。子集 TSP 在目标数 $\le 12$ 时用 Held-Karp 精确求解，超过时用贪心近似，逐量以 `*_exact` 字段标注。数据集中预计算的 `shortest_path_distance` 仅作无 navmesh 时的兜底，不参与正式计算。

### 3.3 诊断层之二：目标记忆（RTSR）

**目标重复选择率（Repeated Target Selection Rate, RTSR）**

$$RTSR=\frac{m-|unique(S)|}{m}$$

设 agent 发出找到信号对应的目标实例序列为 $S=(s_1,...,s_m)$，$s_i$ 为真实目标实例 ID；$m=0$ 时记 0。RTSR 单独隔离"重复认领已找到目标"这一类状态追踪失败——Precision 把它与误识别统一计入 FP，RTSR 把它单独统计，两者相关但不冗余。这一维度在 MultiON/GOAT 中不存在（目标顺序给定，无需维护已完成集合）。

### 3.4 诊断层之三：停止决策（Stopping Regret，仅 MOC）

把"该停不停"与"不该停就停"统一成一个值。设成功发现的时间步升序为 $report\_steps$，预算为 $T$，实际执行 $steps\_taken$ 步：

- **找齐 + 主动终止**：$T_{waste} = finish\_step - $ 最后一次成功发现步（找齐后还在逛）。
- **找齐 + 被截断**（未发完成信号）：$T_{waste} = steps\_taken - all\_required\_found\_step$，语义同上，终点是截断点。
- **没找齐**：$regret = missing \times avg\_cost$，其中 $avg\_cost$ 是相邻成功发现的平均步数间隔；本 episode 不足 2 次发现无法直算时，依次回退到同场景同模式、全局同模式的平均发现成本，都不可得则记不适用。

每个 episode 同时输出原始步数与归一化值（除以 $T$，**不封顶**，允许超过 1）；聚合时合并均值、$T_{waste}$ 分量均值、$regret$ 分量均值分开报告，并给出"没找齐"中主动提前终止与被动截断的占比——前者是停止决策失误，后者是预算不足，必须能拆开看。

### 3.5 诊断层之四：算力代价（Cost）

token 数（分输入/输出、分角色）、模型调用轮数、美元成本**统一在 API 调用平台（网关）侧统计**，评测代码不做记录与换算；评测结果只保留每 episode 的墙钟时间 `episode_elapsed_seconds`。Cost 不参与排名，只作为独立效率维度用于"能力 vs 代价"前沿分析。

### 3.6 诊断层之五：发现池与 $U_t$（需 agent 配合）

对实现了可选接口 `get_target_pool()` 的 agent（如我们的 VGGTNavAgent），评测器每步采集 agent 已实例化的候选实例（位置 + 是否已上报，agent 侧自行转到世界坐标），按位置与 ground truth 合法实例做最近邻匹配（阈值默认 2.0m，可调）：

- **$U_t$**：每个决策时刻已实例化但未上报的合法目标数，逐步记录序列并汇总均值。它是**数据集质量/诊断指标**——用来判断场景是否真的构成"选择"——不参与模型排名。
- **发现池并集**：即 3.2 中 $l^{pool\text{-}optimal}$ 的输入，支撑池质量与新 TSE。

第三方黑箱 agent 没有此接口时，$U_t$、池质量、TSE 均为 null，其余指标不受影响。

### 3.7 离线分析：规模伸缩曲线（Scaling Curve）

独立脚本（`evaluation/main/analysis/`）对评测结果 JSON 做二次分析：把 SR 与 SPL-multi 分别作为目标数量 $k$/$N$ 和场景面积（一次性从 navmesh 离线推导并缓存）的函数，输出分桶均值、回归斜率、多元回归（剥离面积混淆）、分段回归检测"崩坏点"（含 bootstrap 置信区间），并对样本量不足的桶明确标注低置信。

### 3.8 聚合方式

所有指标先逐 episode 计算，再聚合：

- **宏平均**（默认）：逐 episode 取算术平均，每个 episode 权重相等。不适用（null）或缺失字段的 episode 一律跳过；某指标在聚合范围内完全无数据时输出 null（终端显示 N/A），与真实 0 严格区分。
- **微平均**（P/R/F1 与 RTSR 同时报告）：先跨 episode 汇总计数再算比率，目标多的 episode 权重更大。
- **按模式拆分**：`any` 只报 SR 和 SPL；`many`/`all` 分别报告全套指标。
- **主榜单**（SR、F1、SPL-multi）只聚合 `many`/`all` episode；`any` 的结果单独列出，不混入总指标。
- **结果文件**：完整结果保存在单个 JSON（`{"metrics": 聚合指标, "episodes": 逐 episode 全字段记录}`），终端只打印进度与汇总。

## 4. 与现有 Benchmark 的对比

用状态空间大小做更形式化的论证：

MultiON/GOAT 本质是线性序列执行问题，决策空间随目标数量线性增长；本 benchmark 的 MOS 变体 A/C 是子集选择 + 路径规划的组合问题，决策空间随候选目标数量组合爆炸增长（$\binom{M}{k}$ 种子集选择 × 每种子集的排列可能），这是"决策空间更大、更能测出规划能力"这一论断的定量依据。

## 5. 小结

- **测评搜索规划能力**：模型需要自主决定搜索顺序和路线，而不是执行给定序列
- **测评状态追踪能力**：模型需要同时记录"去过哪里"和"找到过哪些目标"，对 working memory 有更高要求
- **更贴近真实任务**：现实中的导航指令（"找到所有的杯子"）天然是无序的
