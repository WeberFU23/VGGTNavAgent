这个工作包括一个多目标具身导航benchmark（/home/wenbofu/habitat_benchmark/多目标导航.pdf和/home/wenbofu/habitat_benchmark/README.md）和针对多目标任务的系统方法（/home/wenbofu/vggt_nav_agent/AGENT_ARCHITECTURE.md）。阅读相应的文件，了解工作内容。


version1.0 VGGT-SLAM+FOUNDIT
一、Benchmark：多目标导航评测（habitat_benchmark/）                                      
                                                                                            
   动机：MultiON、GOAT 这类现有 benchmark 把"多目标"简化成"按给定顺序的多次单目标导航"——顺序
   和数量都给定，决策空间小。本 benchmark 去掉了顺序和数量提示，专门测三种现有 benchmark 覆 
   盖不到的能力：                                                                           
                                                                                            
   1. 搜索规划：自主决定访问顺序与路线（类 TSP / Orienteering 组合优化）                    
   2. 状态追踪：维护空间记忆（去过哪）+ 目标记忆（找到了哪些、是否重复）                    
   3. 终止决策：不知道目标总数时自主判断"是否找全"                                          
                                                                                            
   任务：基于 HM3D val 场景，4 种 target_mode：                                             
   • single / any：主要用于 smoke test 和消融                                               
   • many（MOS，多目标搜索，数量 k 已知）                                                   
   • all（MOC，找全，数量未知、需主动 FINISH）                                              
                                                                                            
   主榜单只聚合 many/all，报 sr / f1 / spl_multi，外加诊断指标（precision/recall、PE/TSE 拆 
   分路径效率、FTR/OER 终止诊断、RTSR 目标记忆）。                                          
                                                                                            
   接口：GOAT-style API 加 MOC 扩展。Agent 每步收                                           
   Observation（rgb/depth/gps/compass/goal_text/target_mode...），返回动作；关键语义是      
   6=TARGET_FOUND（登记发现、不结束 episode、不告知下一个目标）和 0=FINISH（主动终止）。目标
   判定按 GOAT 口径：到目标 navigation view point 的 geodesic 距离 ≤ 0.25m。                
                                                                                            
   二、Agent 系统：vggt_nav_agent                                                           
                                                                                            
   一个针对上述 benchmark 的 RGB-only 多目标导航系统，client/server 双进程架构：            
                                                                                            
   • Server（python3.11）：VGGT-SLAM 做建图定位（关键帧→子图→VGGT 前向+因子图优化，含回环   
     ）；CLIP 常驻做语义检索；SAM3 查询时懒加载做实例分割                                   
   • Agent（python3.9，habitat 内）：不用仿真器 GPS/深度，frontier 探索（骨架拓扑+A*），周期
     性地 ground_object(目标短语) → CLIP top-K 关键帧 → SAM3 分割 → 投影到点云得 3D 目标 →  
     导航到点后 360° 扫描确认 → TARGET_FOUND                                                
   • 多目标状态机：instance memory（confirmed/visited/黑名单）+ belief + 开放路径 TSP 排序 +
     规则式 _should_finish                                                                  
                                                                                            
   实测主要弱点（按影响排序）：                                                             
                                                                                            
   1. CLIP 检索弱：top-K 分数压在 0.21–0.25 平坦区间，含目标帧和无关帧分数几乎不可分 → 大量 
      假阳性                                                                                
   2. 探索无语义引导：大量零命中真相是"没走到"而非"没识别"（起点 3.2m 外就是目标却一路走错方
      向）                                                                                  
   3. VLM 决策层缺位（当前禁用）：无法理解 exactly two、gray fabric 这类属性约束，无候选仲裁
      ，FINISH 保守跑满步数                                                                 
   4. SAM3 措辞敏感 + 单一阈值 0.5 无校准                                                   
   5. SLAM Sim(3) 对齐误差 0.33–1.0m，压在 0.75m 实例合并 / 0.8m 到达判定阈值边缘           
                                                                                            
   当前成绩：单点 episode（起点旁即目标）F1=1.0，但 many 模式（ep0000005）多次重跑零命中。  
                                                                                            
   三、两者的关系                                                                           
                                                                                            
   Benchmark 是"考题"，vggt_nav_agent 是"答卷"——架构文档里的弱点正好对应 benchmark 设计要测 
   的能力维度：CLIP/SAM 弱点影响 TP/FP（Precision）、探索无先验影响 SPL-multi/TSE、         
   _should_finish 规则粗糙影响 FTR/OER、instance memory 影响 RTSR。下一步候选方向是：开放词 
   汇检测器（OWL-ViT/Grounding-DINO）替代或补充 CLIP、探索方向先验、启用 VLM 决策闭环、SAM  
   类别级阈值校准。  
   
   
   
   
version2.0

末端用 VLM-in-the-loop 视觉伺服。 benchmark 卡的是最后 0.25m，而 SLAM 误差是 0.33–1.0m——让最后几米不看坐标、看图像（"目标在画面里越来越大/居中"直到满足到达条件），把末端精度和全局 SLAM 误差解耦。这和你们的两段式兜底是同一个机制，正好复用。


跑一个 oracle ablation。 用 GT pose + GT depth 替换 VGGT 跑一遍，对比成功率差多少。这个实验直接回答"几何误差贡献了多少失败"：如果差距大，说明该投资源改善 SLAM（子图对齐、回环），VGGT 不是要不要的问题而是要升级的问题；如果差距小，说明 VGGT 已经够用，瓶颈在语义层，安心做语义记忆。
