# 论文大纲与实验叙事

## 1. 论文定位与主线

**核心贡献是 benchmark；agent 是基线并提供诊断。**

叙事主线：任务提出自主目标选择、任务进度维护与完成判断需求 → 指标定义如何量化agent的能力 → agent 提供baseline → 实验展示性能、决策机会和具体瓶颈。

本文保留五项核心分析：主结果、规模变化、MOC 停止分解、发现池与路径诊断、轨迹案例；补充三项行为分析：`U_t`、目标非贪心与离线路线比较、预算分段动作分布。

下文的“结论”均指待实验回答的问题和允许的表述范围，不预设结果方向。

## 2. 正文章节安排

### 1. Introduction

- 接待客人时 “找3个椅子”  和洗衣服前 “找到所有衣服” 这样的现实任务需求，引出目标子集选择、访问排序、实例追踪和未知数量下的终止判断。
- 提出 MOS/MOC、结果指标与过程诊断，以及可审计的参考 agent。
- 贡献收敛为三项：任务与数据；评测与诊断体系；参考系统与实证发现。

**作用：** 让读者明确论文要评估什么能力，以及为什么需要同时观察任务结果和决策过程。

### 2. Related Work （todo）

- 比较单目标、顺序多目标、无序多目标导航，之前的benchmark主要关注lifelong（串行），我们补上对搜索规划（并行）的能力测评。
- VGGT的相关内容

### 3. Benchmark

**3.1 任务定义**

- 任务规则定义（观测输入、动作输出、成功标准、agent规格）。
- MOS：数量已知、顺序未知。
- MOC：总数未知，要求找全并主动停止。

**3.2 数据构建与统计**

- 数据集生成pipline：场景来源、目标标注、查询生成、可达性筛选、自然语言生成。
- 数据统计：场景数、episode 数、目标数量、目标分布、目标类型。

**3.3 评测指标体系**

| 层级 | 指标 | 定义目的 |
|---|---|---|
| 主结果 | SR、F1、SPL-multi；补充 Precision/Recall | 衡量成功、报告准确完整性及成功条件下的路径效率 |
| 过程诊断 | 池覆盖、池质量、TSE、OE、LE、RTSR、停止相关量 | 区分总分背后的不同表现特征 |
| 行为证据 | `U_t`、目标非贪心与路线比较、预算分段动作分布 | 观察选择机会、首选目标价值和行动调整 |

- 正文给出核心定义；求解近似、匹配阈值、缺失值和聚合规则放附录。

**作用：** 先定义测量对象，再展示结果，避免根据参考 agent 的表现临时解释指标。

### 4. Reference Agent

按任务需求介绍机制，不按代码模块逐项展开：

| 任务需求 | 参考 agent 机制 |
|---|---|
| 搜索与重访 | 几何/语义 frontier、caption 检索 |
| 身份与完成集合维护 | Observation / Instance / ReportClaim 三层记忆 |
| 规划与决策 | 基于 world-state 的事件驱动 VLM 决策 |
| 可靠执行与可分析性 | 几何执行器、决策/工具/报告日志 |

- 放一张架构图，说明哪些判断由 VLM 完成、哪些状态和约束由 harness 提供。
- 尺度恢复、自由空间处理和工程阈值放附录。

**作用：** 说明基线如何求解任务，以及后文诊断证据如何产生。

### 5. Experimental Setup

- 固定 agent、模型配置、数据子集和预算；报告场景覆盖、任务分组、模型调用设置与版本。
- 主评测覆盖 MOS/MOC、多个目标数量和多个场景；不必做所有维度的全组合。
- 若预算允许，增加第二个决策 VLM，在相同 episodes 上配对比较；固定 caption、建图、SAM 和工具预算。
- 两模型比较同时涉及高层选择与核验等判断，不全部归因为规划。只有一个模型时，不声称验证了跨模型区分能力。
- 结果报告有效样本量和置信区间；同场景/同 episode 内样本相关，避免将所有决策步当成独立样本。

**作用：** 明确实验结论的适用条件；以下分析主要复用这一批运行，不各自开新实验。

### 6. Results and Analysis

| 小节 | 放哪些实验与指标 | 推荐呈现 | 结论与指标的作用 |
|---|---|---|---|
| **6.1 Overall Performance** | MOS/MOC 分别报告 SR、F1、SPL-multi、P/R、样本数；可附耗时 | 主结果表；有第二模型则配对比较 | 建立参考性能；用部分完成指标区分“同样失败但完成程度不同”。单系统低分不能独自证明 benchmark 有效或普遍困难 |
| **6.2 Scaling with Task Difficulty** | 按目标数量、场景规模、描述类型分组的 SR、Recall、SPL-multi | 少量分桶曲线及置信区间 | 展示性能如何随任务条件变化；控制/分层报告面积、密度和预算，避免混淆。样本不足时不做复杂崩坏点结论 |
| **6.3 Completion versus Stopping in MOC** | 找齐率、正式 SR；找齐并停止/找齐被截断/未找齐主动停/未找齐被截断四类比例；找齐后额外步数、提前停止遗漏数；Stopping Regret 作补充 | 四类结果分布 + 停止成本图 | 分开观察搜索完成与宣布完成；原始量解释问题，Regret 概括代价。未找齐被截断不能直接归因为停止错误；找齐后继续探索不一定意味着当时已有充分停止证据 |
| **6.4 Discovery and Route Diagnostics** | 池覆盖不足率、池质量、TSE、OE、LE、RTSR | 分组分布或诊断表 | 展示搜索发现、已知候选选择、排序及重复报告特征。池不足/未完成/近似求解单独标识；LE 包含探索开销，不能直接等同低层执行能力；四因子不作为因果失败占比 |
| **6.5 Target Choice Opportunities and Value** | `U_t` 机会覆盖率；目标非贪心率；离线首选路线比较的胜/平/负及代价差 | 机会覆盖图 + 路线代价差分布 | `U_t` 说明实际出现多个候选；路线差异说明选择具有代价后果；比较 VLM 首选是否比最近目标更有利。具体口径见第 3 节 |
| **6.6 Budget-Related Action Changes** | 按原始预算剩余比例统计探索、目标访问、核验/调整、报告、主动停止；MOS/MOC 分开 | 分段动作分布 + 样本量；必要时按进度分层 | 展示行为是否随预算阶段变化。进度和候选条件相近时仍有变化，支持与预算压力相关的调整；不声称单凭分布变化证明因果或更优策略 |
| **6.7 Trajectory Case Studies** | 决策、工具、实例、报告与终止时间线；选取目标选择、重复识别、提前停、找齐后探索、几何/感知失败案例 | 少量带图时间轴 | 将抽象指标对应到实际行为，为前述统计提供解释。按预先说明的案例类型选取，不用个例替代总体结论 |

篇幅紧时将 6.7 的案例穿插到 6.3–6.6；预算行为图和额外分组可放附录，正文保留主要发现。

### 7. Discussion and Conclusion

- 回答任务是否出现了自主选择机会、这些选择有何路线价值，以及当前系统有哪些可观察瓶颈。
- 说明诊断的边界：候选池受表示/定位影响，离线评价不等于另一策略闭环成绩，观察关联不等于因果。
- 总结 benchmark、参考实现和实证发现；不以未做的结构消融支撑方法优越性。

## 3. 三项行为分析的固定口径

### A. `U_t`：实际目标选择机会

- 定义：时刻 t 已实例化、匹配合法目标且尚未报告的目标数。
- 报告出现 `U_t >= 2` 的 episode 比例，以及高层决策时刻比例；可附 `U_t >= 3` 子集。
- 原始 `u_t_series` 按环境步采集，需与高层决策时刻对齐；不把连续路径执行重复计为独立选择，排除 MOS 配额完成后的尾段。
- 从“池内有多个目标”进一步筛出当前可选、可达且任务相关的目标选择事件，并报告筛选后的覆盖率。
- `U_t` 不覆盖仅存在于 caption/视觉记忆、尚未实例化的目标；低值不能证明任务没有规划空间。

### B. 目标非贪心与离线首选路线比较

**问题：在当时已知的目标中，VLM 的首选是否比最近目标更有利于完成剩余访问？**

1. 取至少两个尚未报告候选的目标访问决策，冻结最终动作前最新的当前位置、候选集、已报告集合及剩余配额。工具写入可能更新状态，不能只取本轮最初输入。
2. 以当时提供的路径代价定义最近目标，与 VLM 所选目标比较；对并列/近似并列设置固定容差。排除强制改写与 fallback。
3. 对路线比较，构造两种首选共用的固定距离图，明确使用估计几何还是评测几何，以及报告位置/到达范围口径。不得纳入未来新发现的目标。
4. 计算 `J_t(a)`：从当前位置先访问 a，再在固定池内完成剩余访问要求的最短总路程。两种首选使用相同的后续优化方式。
5. 计算 `Delta_t = J_t(最近目标) - J_t(VLM目标)`：正值表示 VLM 首选更好，负值表示最近目标更好，容差内为持平。

| 任务条件 | 路线比较口径 |
|---|---|
| MOS，候选池覆盖剩余配额 | 固定首选后，优化剩余子集与顺序，并满足类别配额 |
| MOS，仅剩一个目标需求 | 单独报告；同样合法可达时最近目标对纯距离目标已最优 |
| MOS，池覆盖不足 | 报告比例，跳过完整剩余任务路线比较，不把较少目标的路线与完整任务混比 |
| MOC | 访问冻结池内所有尚未报告目标；只解释已知目标的排序，不评价未来探索或停止 |

- 报告有效机会/可比较样本覆盖率、目标非贪心率，以及非贪心事件中的胜/平/负比例和 `Delta_t` 分布。
- 错误实例、不可达或缺少距离的选择单独计数，不能无说明删除；如用真值合法性筛选，明确为条件化诊断。
- 优先在可精确求解的候选规模上做，近似解单独标识；按 episode/场景处理重复决策的相关性。
- **允许结论：首选对应的可实现路线代价更好/更差。禁止结论：VLM 后来实际走出了该最优路线，或完整贪心 agent 的 SR/F1/SPL 更好/更差。**

### C. 预算相关动作变化

- 按 `steps_remaining / max_steps` 分四段，以原始预算为分母，不按实际结束时间归一化。
- 统计高层决策事件，统一动作类别；工具核验若单独分析，应使用独立分母，不与高层动作频次混算。
- MOS/MOC 分开，报告每段有效决策数及 episode 数；注意晚段只剩长轨迹的样本构成变化。
- 样本允许时按任务进度、可选实例/frontier 数和决策事件类型分层；MOS 排除配额完成后的尾段，MOC 可使用当时可见的已报告数等状态。
- 主动 FINISH 与预算/超时截断分开，VLM 原始提议与 harness 改写动作分开；不把评测器截断当模型决策。
- 结论表述为“与预算阶段/压力相关的行动调整”；分布不变也不能证明预算没有作用。

## 4. 运行与材料清单

- **闭环运行：** 完整参考 agent 的主评测；预算允许时补一个决策 VLM 的配对运行。
- **日志离线分析：** 本文八项分析；路线比较需额外离线求解，规模分析可能需要 navmesh 面积缓存，不需模型调用。
- **暂不运行：** frontier/记忆/SAM/尺度等结构消融、完整贪心 agent、多预算参数扫描。
- **归档：** 逐 episode 结果 JSON、`decision_trace.jsonl`、含完整 prompt 的 `vlm_calls.jsonl`、配套图像与动作日志，以及模型/配置/代码版本和 run/episode 标识。
- **数据依赖：** `decision_trace` 只有状态摘要，目标代价与最新候选应从完整 VLM 输入恢复；静态路线计算还需当时位置、候选坐标/身份及相应距离信息。缺失时报告不可分析数量，不用未来状态补齐当时知识。

相关设计：[BENCHMARK_DESIGN.md](BENCHMARK_DESIGN.md)、[AGENT_ARCHITECTURE.md](AGENT_ARCHITECTURE.md)。
